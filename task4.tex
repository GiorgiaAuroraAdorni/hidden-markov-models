\Problem{
	You are given three data points $(x, y) : (-1, 0), (0, 1), (1, 0)$. We are 2
	using squared error loss function $l(y, \hat y) = (y - \hat y)$.
	\begin{itemize}
		\item[(a)] What is leave-one-out cross-validation error of constant 
		model $f(x) = c$?
		\item[(b)] What is leave-one-out cross-validation error of linear model 
		$f(x) = ax + b$?
	\end{itemize}
}

\Solution{
	\begin{itemize}
		\item[(a)] 
		\item[(b)] 
	\end{itemize}
}